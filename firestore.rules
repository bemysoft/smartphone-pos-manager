rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic allow all for now since the server uses the client SDK 
    // and we just need it to work quickly without full auth setup
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
